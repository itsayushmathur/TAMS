const getProjectAllocation = async (projectId) => {
  let response;

  return response;
};

const getMyAllocation = async (talentId) => {
  let response;

  return response;
};

export { getProjectAllocation, getMyAllocation };
